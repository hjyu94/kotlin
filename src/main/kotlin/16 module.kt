/*
    # 모듈
    - kt 파일들을 모아 관리하는 개념 -> 패키지
    - 패키지를 모아 관리하는 개념 -> 모듈

    프로젝트 > 모듈 > 패키지 > kt 파일

    - 모듈끼리는 독립적이므로
      한 모듈은 안드로이드, 한 모듈은 자바, 다른 모듈은 코틀린 이런 방식의 설계가 가능하다.

    - 모듈 등록: project structure - dependencies - + - module dependency
      module1 에서 module2 를 등록해주면 다른 모듈에 있는 패키지, 클래스 등을 사용할 수 있어진다.
 */

